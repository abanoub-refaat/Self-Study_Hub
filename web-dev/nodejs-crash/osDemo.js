import os from "os";

// userInfo()
console.log(os.userInfo());

// totalmem(): return the total memory in bytes
console.log(os.totalmem());

// freemem(): return the free memory in bytes
console.log(os.freemem());

// cpus(): return the number of CPUs
console.log(os.cpus());
